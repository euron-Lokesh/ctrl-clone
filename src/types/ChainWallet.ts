import { LucideIcon } from "lucide-react";

export interface Blockchain {
  name: string;
  color: string;
  icon: LucideIcon;
}

export interface ItemStyle {
  transform: string;
  opacity: number;
  zIndex: number;
}
